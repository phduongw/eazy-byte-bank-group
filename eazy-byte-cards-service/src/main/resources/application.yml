server:
  port: 9000
spring:
  application:
    name: CARDS
  datasource:
    url: jdbc:mysql://localhost:3306/cards-db
    username: root
    password: 3gAn@130599
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: true
    defer-datasource-initialization: true
  sql:
    init:
      mode: always
  config:
    import: optional:configserver:http://localhost:8071/
  #    import:
#      - "application_qa.yml"
#      - "application_prod.yml"
  profiles:
    active: "prod"
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
management:
  endpoints:
    web:
      exposure:
        include: "*"
