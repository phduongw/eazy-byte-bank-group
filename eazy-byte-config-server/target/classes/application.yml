spring:
  application:
    name: CONFIG-SERVER
  profiles:
    active: git
  cloud:
    config:
      server:
#        native:
#          search-locations: "classpath:/config"
          git:
            default-label: main
            timeout: 5
            clone-on-start: true
            force-pull: true
            uri: https://github.com/phduongw/eazy-byte-file-config.git
#  rabbitmq:
#    host: localhost
#    port: 5672
#    username: guest
#    password: guest
encrypt:
  key: "219238ASJDNZ&=7NA*&%%12312"

server:
  port: 8071

management:
  endpoints:
    web:
      exposure:
        include: "*"
  health:
    readinessstate:
      enabled: true
    livenessstate:
      enabled: true
  endpoint:
    health:
      probes:
        enabled: true
